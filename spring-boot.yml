---
- name: Spring Boot
  hosts: "{{ target | default('localhost') }}"
  become: yes
  roles:
  - { role: ansible-springboot-role, 
      spring_boot_file_source_local: 'https://github.com/genekuo/ansible/tree/master/bootrest/target/bootrest-1.jar' 
    }
